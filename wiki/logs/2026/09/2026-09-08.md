---
date: 2026-09-08
timezone: Asia/Shanghai
status: active
---

# 2026-09-08

## 20:46 CST — Retrospective summary of today's NFs work

> [!note] Timestamp scope
> This entry was recorded at 20:46 CST. Earlier changes in the same task did not have individual timestamps, so they are summarized here without invented times.

### Changed

- Created the initial Obsidian-compatible research wiki and linked its project, theory, physics, model, preprocessing, dataset, validation, experiment, decision, infrastructure, and open-question clusters.
- Added the canonical [[NFs Pipeline Architecture]] design covering the FS-to-NFs migration, pipeline stages, composition over inheritance, configuration layering, output layout, extension points, hyperparameter tuning, and definition of done.
- Created the clean NFs package foundation with project metadata and repository rules.
- Added frozen dataset/campaign references for 2022–2025.
- Added maintained specifications for [[Preprocessing A]], [[Preprocessing B]], and [[Preprocessing C]].
- Added the Model 4 baseline and nine selectable named tuning trials.
- Added guard v2 policy configuration, nine pipeline stage contracts, and execution plans for baseline, tuning, generated-validation, final test, and 100k-event MuonDIS delivery.
- Added deterministic configuration composition and hashing.
- Added safe short run IDs and canonical artifact path construction.
- Added atomic run manifests and a central `runs.csv` index generator.
- Ported the first validated scientific component: Model 4 weighted NLL and sample-weight statistics.
- Added `MIGRATION_MANIFEST.yaml` tied to FS commit `9df6e2d`, source checksums, migration status, and hashes of all four frozen selected-model files.

### Main files

- `NFs/README.md`
- `NFs/AGENTS.md`
- `NFs/pyproject.toml`
- `NFs/MIGRATION_MANIFEST.yaml`
- `NFs/src/flashsim_nf/config.py`
- `NFs/src/flashsim_nf/naming.py`
- `NFs/src/flashsim_nf/artifacts.py`
- `NFs/src/flashsim_nf/manifest.py`
- `NFs/src/flashsim_nf/models/model4/objective.py`
- `NFs/configs/`
- `NFs/pipelines/model4/`
- `NFs/tests/`
- `NFs/wiki/`

### verification

- 12 targeted tests passed.
- Ruff passed.
- All 30 initial YAML files parsed as mappings.
- Model 4 weighted NLL and sample-weight summaries match the frozen FS implementation.
- Wiki lint reported zero broken links and zero orphan pages before this daily-log addition.

### Decisions and notes

- Preserve validated scientific behavior from FS with equivalence tests.
- Write orchestration, manifests, naming, registry, and path management as clean new code.
- Keep historical Model 1/2 workflows in FS rather than copying them into NFs.
- Use `flashsim_nf` as the Python package name while retaining `NFs` as the project folder.
- Use stable machine IDs and separately editable display names.
- Keep existing FS data and outputs unchanged during migration.

### Next

- Port data/provenance contracts and ROOT I/O.
- Port [[Preprocessing A]] first and compare its transform/inverse numerically with FS.
- Continue with `drop_z_E` reconstruction, guard v2, generation, and evaluation only after equivalence gates pass.

Related: [[Current Research Status]], [[Source and Provenance]], and [[Model 4]].

## 21:18 CST — Composed preprocessing and safe epoch-450 resume

### Changed

- Implemented [[Preprocessing A]], [[Preprocessing B]], and [[Preprocessing C]] as compositions of small invertible transform components rather than an inheritance hierarchy.
- Preserved the frozen FS numerical behavior for forward and inverse transforms.
- Changed Model 4 defaults to 500 maximum epochs, no early stopping by default, and full-state checkpoints every 50 epochs.
- Added a non-destructive branch-resume mechanism that requires a new run directory, verifies an epoch-450 source by hash, restores available state, starts at epoch 451, and records missing-state consequences.
- Added checkpoint inspection and resume-branch preparation commands.
- Added full future checkpoint state including model, optimizer, scheduler, optional scaler, early-stopping state, best validation loss, optional best weights, and RNG state.

### Main files

- `NFs/src/flashsim_nf/preprocessing/`
- `NFs/src/flashsim_nf/training/`
- `NFs/scripts/inspect_training_checkpoint.py`
- `NFs/scripts/prepare_resume_branch.py`
- `NFs/tests/test_preprocessing_equivalence.py`
- `NFs/tests/test_branched_resume.py`
- `NFs/configs/models/model4/baseline.yaml`
- `NFs/wiki/experiments/Epoch 450 Branched Resume.md`

### Verification

- Ruff passed.
- 21 targeted NFs tests passed.
- A/B/C transforms and inverses match the frozen FS implementation numerically.
- A toy epoch-450 checkpoint resumed at epochs 451 and 452 in a new directory without changing the source checkpoint.

### Decision or note

- No epoch-450 checkpoint exists in the downloaded local Model 4 outputs; the local legacy checkpoints end at or before epoch 200.
- Legacy `last_model.pt` restores optimizer, scheduler, early-stopping state, and best loss, but lacks scaler/RNG/embedded best-model state.
- The actual EOS epoch-450 checkpoint must be inspected before submission.

### Next

- Port the Model 4 NF backend and ROOT data adapter into the callback-driven trainer.
- Inspect the actual EOS epoch-450 payload and create its resolved new-run configuration.

## 21:56 CST — Fresh-run checkpoint policy and controlled tuning planner

### Changed

- Extended the callback-driven trainer to support both fresh and branched-resume runs.
- Implemented `best_model.pt` on every validation improvement, `last_model.pt` every epoch, and full-state numbered checkpoints every 50 epochs for both run modes.
- Added default training-policy parsing for 500 epochs with early stopping disabled; early stopping remains opt-in through config.
- Added a config-driven Model 4 tuning planner for A/B/C and named trial/seed filtering.
- Added fail-closed checks for an explicit baseline, unknown trials, invalid pipelines, and silent multi-parameter overrides.
- Added stable config hashes, short run IDs, immutable run paths, and explicit train/validation/test roles to every plan entry.
- Added the [[Controlled Hyperparameter Experiments]] policy and repository instructions.

### Main files

- `NFs/src/flashsim_nf/training/`
- `NFs/src/flashsim_nf/experiments/tuning.py`
- `NFs/scripts/plan_model4_tuning.py`
- `NFs/tests/test_branched_resume.py`
- `NFs/tests/test_tuning_plan.py`
- `NFs/AGENTS.md`
- `NFs/wiki/experiments/Controlled Hyperparameter Experiments.md`

### Verification

- Fresh-run checkpoint test confirmed best at epoch 1, full periodic state at epoch 50, and last at epoch 51.
- Controlled planner test confirmed 27 default runs from A/B/C times nine trials.
- A two-run preview produced distinct stable IDs and output paths for baseline and `lr_hi`.

### Decision or note

- Periodic checkpoint frequency does not limit best-epoch selection.
- Resume/continuation remains a separate experiment type and must not be ranked as a fresh hyperparameter candidate.

### Next

- Connect the training session to the ported RQ-spline NF backend and verified ROOT data adapter.

## 22:12 CST — ROOT-to-checkpoint Model 4 vertical slice

### Changed

- Ported the RQ-spline NF backend and verified its graph, parameters, and log probability against the frozen FS implementation.
- Added a fail-closed ROOT adapter that aligns preprocessed kinematics with raw FLUKA weights using source metadata and frozen split provenance.
- Connected the adapter and backend to real weighted-NLL train and validation epochs, optimizer, scheduler, optional AMP, checkpointing, and branch resume.
- Corrected the `drop_z_E` example contract to six NF inputs: `(x, y, pz, px, py, t)`.
- Added train and validation weight summaries, including total weight and effective sample size, to each resolved run config.
- Persisted the exact best epoch alongside best validation loss in checkpoints, training summaries, and resume-state reports.

### Files

- `NFs/src/flashsim_nf/models/flows/`
- `NFs/src/flashsim_nf/data/root.py`
- `NFs/src/flashsim_nf/training/model4.py`
- `NFs/scripts/train_model4.py`
- `NFs/configs/jobs/model4_training.example.yaml`
- `NFs/tests/test_flow_backend_equivalence.py`
- `NFs/tests/test_root_adapter.py`
- `NFs/tests/test_model4_training_vertical.py`
- `NFs/MIGRATION_MANIFEST.yaml`
- `NFs/wiki/project/Model 4 Training Vertical Slice.md`

### Verification

- Ruff passed for `src`, `scripts`, and `tests`.
- All 35 NFs tests passed, covering preprocessing, flow, ROOT, checkpoint, tuning, and vertical training.
- The tiny vertical run resumed an epoch-2 full-state checkpoint at epoch 3 in a different run directory.

### Decision or note

- This proves the clean local implementation path; it does not claim that an EOS production checkpoint or the actual epoch-450 checkpoint has been resumed.
- The canonical FS and EOS data/output trees were not modified.

### Next

- Resolve one real frozen campaign config with verified paths and hashes, then run a controlled NFs smoke comparison against canonical FS evidence.

## 23:09 CST — Train-only versus all-FLUKA robust guard comparison

### Changed

- Added weighted robust-reference guard fitting with separate train-only selection and all-clean-splits production roles.
- Made the all-reference artifact self-identify as `production_only` and `selection_allowed: false`.
- Added same-proposal comparison metrics, reference coverage by split, non-exclusive per-feature reasons, rejection overlap, and a compact physical-bound table.
- Used float64 ROOT values for guard statistics while retaining float32 as the Model 4 training default.
- Added a config-driven command that refuses to overwrite an existing output and writes a manifest plus `_SUCCESS.json`.

### Files

- `NFs/src/flashsim_nf/guards/reference.py`
- `NFs/src/flashsim_nf/guards/__init__.py`
- `NFs/src/flashsim_nf/data/root.py`
- `NFs/scripts/fit_compare_reference_guards.py`
- `NFs/configs/guards/reference_scope_comparison.yaml`
- `NFs/configs/guards/guard_reject_v2.yaml`
- `NFs/tests/test_reference_guards.py`
- `NFs/tests/test_root_adapter.py`
- `NFs/wiki/experiments/Train versus All-FLUKA Guard.md`

### Verification

- Ruff passed and nine focused guard/ROOT tests passed before the real-data run.
- Real reference-only artifacts completed for cleaned 2022, 2023, and 2024 under `NFs_data/guards/<dataset>/train_vs_all_clean_v2`.
- Reference rejection was below 0.1% for both scopes in all three available campaigns.

### Decision or note

- `train_ref` remains the only guard allowed for generated-validation and selection.
- `all_ref` is a frozen production/MuonDIS candidate, not evidence for improving validation or held-out-test claims.
- The 2025 local raw train/validation/test split files were unavailable, so no local 2025 guard was fabricated.
- No same-proposal generated comparison was claimed because a verified pre-rejection proposal ROOT was not supplied.

### Next

- Run the same command for 2025 on lxplus, then compare both guards on one verified pre-rejection proposal file per year before connecting an all-reference artifact to production generation.
