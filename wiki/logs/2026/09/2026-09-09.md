# 2026-09-09

## 04:29 CST — Real training resolution and read-only preflight

### Changed

- Added one dataset-driven Model 4 training-config resolver for all maintained years and preprocessing A/B/C.
- Added an immutable config hash, stable run ID, and collision-safe output path.
- Added a fail-closed preflight that validates maintained-source hashes, fitted preprocessing metadata, split provenance, model/training contracts, and fully scans train and validation ROOT files.
- Enforced the cleaned 2022 dataset identity and prevented test ROOT inputs from entering training.
- Added CLI entry points and an operational runbook.

### Files

- `src/flashsim_nf/experiments/training_config.py`
- `src/flashsim_nf/training/preflight.py`
- `scripts/resolve_model4_training_config.py`
- `scripts/preflight_model4_training.py`
- `tests/test_training_config_preflight.py`
- `wiki/project/Training Resolution and Preflight.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff passed for the new resolver, preflight, CLIs, and tests.
- Seven targeted resolver/preflight tests passed.
- A real read-only Pipeline B smoke preflight passed for 2023: 849,713 train rows and 182,082 validation rows were fully scanned; the test set was not loaded.

### Decision or note

- Expected row counts always come from the selected dataset config; they are not shared across years.
- Preflight prints its report to standard output and writes no report or run artifact.
- Preflight hashes the fitted joblib but does not unpickle legacy code.

### Next

- Resolve and preflight the intended first smoke config on lxplus, inspect the JSON gate, then start training only if `training_allowed` is true.

## 05:23 CST — Restore lxplus Python 3.9 compatibility

### Changed

- Replaced the runtime-evaluated Python 3.10 union type aliases in the training session with Python 3.9-compatible typing aliases.
- Declared Python 3.9 as the package and Ruff target because the CERN virtual environment runs Python 3.9.

### Files

- `src/flashsim_nf/training/session.py`
- `pyproject.toml`
- `wiki/project/Training Resolution and Preflight.md`

### Verification

- The preflight import path no longer evaluates `type | GenericAlias` at module import time.

### Decision or note

- This is a compatibility-only change; it does not alter training, preprocessing, model, or checkpoint behavior.

### Next

- Push the compatibility commit, pull it on lxplus, and rerun the same preflight command.

## 05:32 CST — EOS-backed Model 4 Condor submission

### Changed

- Added a pure HTCondor submission builder with validated resources, paths, run identity, and collision checks.
- Added a one-config submit CLI that always completes read-only preflight before printing or submitting a job.
- Added an EOS-backed Condor description and worker launcher for the clean NFs trainer.
- The worker checks its Python/GPU environment and repeats preflight before creating the training run.

### Files

- `src/flashsim_nf/execution/condor.py`
- `scripts/submit_model4_training.py`
- `condor/submit_model4_training.sub`
- `condor/run_model4_training.sh`
- `tests/test_condor_submission.py`
- `wiki/project/Training Resolution and Preflight.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff and shell syntax checks passed.
- Ten targeted submission and preflight tests passed.
- A real 2023 resolved config completed the combined preflight plus Condor dry run without creating its run or log directory.

### Decision or note

- `--dry-run` performs the full scientific gate but does not submit or create runtime directories.
- Actual CERN submission requires `module load lxbatch/eossubmit` because project, input, log, and output paths reside on EOS.

### Next

- Push and pull the new submission files, run the 2025 smoke dry run, then submit its single GPU job.

## 05:39 CST — Update Condor output handling for CERN

### Changed

- Removed `stream_output` and `stream_error` from the Model 4 submit description because CERN EosSubmit no longer accepts streamed stdout/stderr.
- Added a regression test that rejects either obsolete option.

### Files

- `condor/submit_model4_training.sub`
- `tests/test_condor_submission.py`
- `wiki/project/Training Resolution and Preflight.md`

### Verification

- Ruff and shell checks passed.
- Four targeted Condor submission tests passed.

### Decision or note

- The failed attempt was rejected at submission time, so no training job or run directory was created.
- Standard Condor `.out`, `.err`, and scheduler log files remain configured on EOS.

### Next

- Push and pull this compatibility fix, repeat the dry run, and submit the same resolved smoke config once.

## 05:54 CST — Live EOS logging and complete baseline matrix

### Changed

- Added worker-side live logging to EOS with a run-specific timestamped filename, independent of removed Condor stdout/stderr streaming.
- Added a baseline matrix resolver for every maintained dataset and preprocessing A/B/C.
- Added one CLI that can create all 12 smoke or production baseline configs, or a selected dataset subset, while verifying matching existing configs.
- Baseline matrix prerequisite resolution is completed before any new config is written, avoiding partial output when an artifact is missing.

### Files

- `condor/run_model4_training.sh`
- `condor/submit_model4_training.sub`
- `scripts/submit_model4_training.py`
- `scripts/create_model4_baseline_configs.py`
- `src/flashsim_nf/execution/condor.py`
- `src/flashsim_nf/experiments/baselines.py`
- `tests/test_condor_submission.py`
- `tests/test_baseline_configs.py`
- `wiki/project/Training Resolution and Preflight.md`
- `pipelines/model4/README.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff, shell syntax, and 14 targeted tests passed during implementation.
- Nine real local smoke configs were resolved successfully for preprocessing A/B/C across the available cleaned 2022, 2023, and 2024 artifacts.
- The CERN 2025 Pipeline B config had already passed full ROOT preflight; 2025 A/C config resolution remains an lxplus action because their complete prepared artifacts are not present locally.

### Decision or note

- Resolved configs are runtime artifacts and remain outside Git because they embed CERN paths and fitted-artifact hashes.
- The first execution batch is the three 2025 baseline smoke jobs. The other nine configs may be created now but are not submitted automatically.

### Next

- Push and pull, create/verify the 12 CERN smoke configs, dry-run the 2025 A/B/C submissions, then submit each 2025 baseline once.

## 06:25 CST — Post-training diagnostics and weighted generated evaluation

### Changed

- Added an immutable training-validation diagnostic stage for completed Model 4 runs.
- Added weighted physical-space generated evaluation with FGD, covariance distance,
  approximate Energy Distance, sliced Wasserstein, weighted marginal distances,
  and two-sided train-defined tail metrics.
- FLUKA train and evaluation references now fail closed unless their ROOT files
  contain the original `w` branch.
- Added separate CLIs for training diagnostics and generated validation/test
  evaluation, keeping held-out test use explicit.

### Files

- `src/flashsim_nf/evaluation/training.py`
- `src/flashsim_nf/evaluation/generated.py`
- `scripts/evaluate_model4_training.py`
- `scripts/evaluate_model4_generated.py`
- `tests/test_training_evaluation.py`
- `tests/test_generated_evaluation.py`
- `wiki/validation/Post-training Evaluation.md`

### Verification

- Ruff and CLI import checks passed.
- Four targeted evaluation tests passed.
- The complete repository suite passed: 58 tests.

### Decision or note

- Tail thresholds and robust normalization widths are learned from train only.
- Low tail ESS is reported as insufficient statistics instead of overstating a
  discrepancy.
- Checkpoint-to-physical-ROOT generation remains the next separate port.

### Next

- Run training diagnostics on completed baseline runs, then port physical sample
  generation before performing generated-validation selection.

## 14:35 CST — Per-epoch training progress in live EOS logs

### Changed

- Added one concise, flushed progress line after every safely written epoch
  checkpoint.
- Live lines include train/validation weighted NLL, best validation NLL and
  epoch, learning rate, gradient norm, batch-weight CV, epoch time, and a new-best
  marker.
- Retained `training/training_history.csv` as the complete structured record.

### Files

- `src/flashsim_nf/training/session.py`
- `tests/test_branched_resume.py`
- `wiki/project/Training Resolution and Preflight.md`

### Decision or note

- Progress is printed only after checkpoint persistence, so a displayed epoch is
  recoverable through `last_model.pt`.
- The worker's existing unbuffered Python and EOS `tee` setup makes each line
  visible immediately without CERN's removed schedd streaming feature.

### Verification

- Ruff passed.
- Nine targeted training and resume tests passed.

### Next

- Push and pull the change before submitting or resuming any new training job;
  already-running jobs keep the code version with which they started.

## 15:40 CST — Non-overlapping italic plot watermark

### Changed

- Moved the training-diagnostics `FlashSim in progress` watermark to the
  lower-right figure corner.
- Changed the watermark to italic and reduced its size so it does not cover
  plotted data.

### Files

- `src/flashsim_nf/evaluation/training.py`

### Verification

- Ruff passed.
- Eight targeted training, resume, and training-evaluation tests passed.

### Decision or note

- This is presentation-only and does not change losses, checkpoints, ROOT
  values, or evaluation metrics.

### Next

- Use the same presentation convention when the physical generated-sample
  plotting backend is ported into NFs.

## 16:42 CST — Complete Model 4 post-training pipeline

### Changed

- Ported checkpoint-to-physical-ROOT generation into NFs without importing the
  old FS package at runtime.
- Added an adapter for the frozen FS preprocessing-v3 metadata so A/B/C inverse
  transforms are composed inside `flashsim_nf` rather than unpickled from
  `preprocessor.joblib`.
- Added train-only scoring-plane fitting, deterministic `z` reconstruction, and
  muon mass-shell `E` reconstruction for `drop_z_E`.
- Added robust guard rejection, finite-domain and physics hard gates, immutable
  `w1`/`global_c` exports, normalization manifests, and full ROOT readback.
- Added generated-validation orchestration, validation-only winner freezing,
  final-test/MuonDIS selection gates, and CERN EOS submission with live logs.
- Extended generated evaluation with weighted HGB C2ST, Pearson/Spearman
  comparisons, combined core/full feature pages, correlation pages, and tail
  CCDF plots.

### Files

- `src/flashsim_nf/generation/`
- `src/flashsim_nf/reconstruction/`
- `src/flashsim_nf/preprocessing/legacy.py`
- `src/flashsim_nf/evaluation/plots.py`
- `src/flashsim_nf/evaluation/generated.py`
- `src/flashsim_nf/pipeline/post_training.py`
- `src/flashsim_nf/data/root.py`
- `src/flashsim_nf/guards/reference.py`
- `scripts/resolve_model4_generation_config.py`
- `scripts/generate_model4.py`
- `scripts/run_model4_post_training.py`
- `scripts/execute_model4_post_training.py`
- `scripts/submit_model4_post_training.py`
- `scripts/select_model4_validation_winner.py`
- `condor/run_model4_post_training.sh`
- `condor/submit_model4_post_training.sub`
- `wiki/validation/Post-training Evaluation.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff passed for the new generation, evaluation, preprocessing, pipeline, and
  CLI code.
- Synthetic checkpoint-to-ROOT generation verified inverse preprocessing,
  physics reconstruction, equal kinematics in `w1`/`global_c`, and constant
  normalization.
- Targeted generation, guard, evaluation, and reconstruction tests passed.

### Decision or note

- Generated-validation uses the train-reference guard; test and MuonDIS require
  an immutable winner selected without test data.
- Dataset detector x/y bounds are not invented from sample extrema. The code
  records them as disabled until an authoritative campaign artifact exists.

### Next

- Push and pull this implementation, resolve one validation generation config
  for each A/B/C baseline, submit them, then freeze one winner per year before
  opening the test stage.

## 19:10 CST — Close the four-year validation matrix workflow

### Changed

- Added a fail-closed resolver that discovers exactly 12 completed baseline
  runs and creates immutable generated-validation configs for A/B/C in all four
  campaigns.
- Added an exact-count matrix submitter with dry-run support.
- Made the single-job post-training submitter create its Condor log directory
  before submission.
- Added the four-year validation commands to the post-training runbook.

### Files

- `scripts/create_model4_validation_configs.py`
- `scripts/submit_model4_post_training_matrix.py`
- `scripts/submit_model4_post_training.py`
- `wiki/validation/Post-training Evaluation.md`

### Verification

- Ruff passed for the complete new post-training implementation.
- The full repository test suite passed: 62 tests.
- Shell syntax and Git whitespace checks passed.

### Decision or note

- Validation jobs use generation seed 1556 and the train-reference guard.
- Test and MuonDIS remain locked until a validation-only winner is frozen.

### Next

- Push the code, pull it on lxplus, create the 12 validation configs, inspect
  their dry-run submissions, and then submit the exact matrix.
