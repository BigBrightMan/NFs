# 2026-09-09

## 04:29 CST — Real training resolution and read-only preflight

### Changed

- Added one dataset-driven Model 4 training-config resolver for all maintained years and preprocessing A/B/C.
- Added an immutable config hash, stable run ID, and collision-safe output path.
- Added a fail-closed preflight that validates maintained-source hashes, fitted preprocessing metadata, split provenance, model/training contracts, and fully scans train and validation ROOT files.
- Enforced the cleaned 2022 dataset identity and prevented test ROOT inputs from entering training.
- Added CLI entry points and an operational runbook.

### Files

- `src/flashsim_nf/experiments/training_config.py`
- `src/flashsim_nf/training/preflight.py`
- `scripts/resolve_model4_training_config.py`
- `scripts/preflight_model4_training.py`
- `tests/test_training_config_preflight.py`
- `wiki/project/Training Resolution and Preflight.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff passed for the new resolver, preflight, CLIs, and tests.
- Seven targeted resolver/preflight tests passed.
- A real read-only Pipeline B smoke preflight passed for 2023: 849,713 train rows and 182,082 validation rows were fully scanned; the test set was not loaded.

### Decision or note

- Expected row counts always come from the selected dataset config; they are not shared across years.
- Preflight prints its report to standard output and writes no report or run artifact.
- Preflight hashes the fitted joblib but does not unpickle legacy code.

### Next

- Resolve and preflight the intended first smoke config on lxplus, inspect the JSON gate, then start training only if `training_allowed` is true.

## 05:23 CST — Restore lxplus Python 3.9 compatibility

### Changed

- Replaced the runtime-evaluated Python 3.10 union type aliases in the training session with Python 3.9-compatible typing aliases.
- Declared Python 3.9 as the package and Ruff target because the CERN virtual environment runs Python 3.9.

### Files

- `src/flashsim_nf/training/session.py`
- `pyproject.toml`
- `wiki/project/Training Resolution and Preflight.md`

### Verification

- The preflight import path no longer evaluates `type | GenericAlias` at module import time.

### Decision or note

- This is a compatibility-only change; it does not alter training, preprocessing, model, or checkpoint behavior.

### Next

- Push the compatibility commit, pull it on lxplus, and rerun the same preflight command.

## 05:32 CST — EOS-backed Model 4 Condor submission

### Changed

- Added a pure HTCondor submission builder with validated resources, paths, run identity, and collision checks.
- Added a one-config submit CLI that always completes read-only preflight before printing or submitting a job.
- Added an EOS-backed Condor description and worker launcher for the clean NFs trainer.
- The worker checks its Python/GPU environment and repeats preflight before creating the training run.

### Files

- `src/flashsim_nf/execution/condor.py`
- `scripts/submit_model4_training.py`
- `condor/submit_model4_training.sub`
- `condor/run_model4_training.sh`
- `tests/test_condor_submission.py`
- `wiki/project/Training Resolution and Preflight.md`
- `MIGRATION_MANIFEST.yaml`

### Verification

- Ruff and shell syntax checks passed.
- Ten targeted submission and preflight tests passed.
- A real 2023 resolved config completed the combined preflight plus Condor dry run without creating its run or log directory.

### Decision or note

- `--dry-run` performs the full scientific gate but does not submit or create runtime directories.
- Actual CERN submission requires `module load lxbatch/eossubmit` because project, input, log, and output paths reside on EOS.

### Next

- Push and pull the new submission files, run the 2025 smoke dry run, then submit its single GPU job.

## 05:39 CST — Update Condor output handling for CERN

### Changed

- Removed `stream_output` and `stream_error` from the Model 4 submit description because CERN EosSubmit no longer accepts streamed stdout/stderr.
- Added a regression test that rejects either obsolete option.

### Files

- `condor/submit_model4_training.sub`
- `tests/test_condor_submission.py`
- `wiki/project/Training Resolution and Preflight.md`

### Verification

- Ruff and shell checks passed.
- Four targeted Condor submission tests passed.

### Decision or note

- The failed attempt was rejected at submission time, so no training job or run directory was created.
- Standard Condor `.out`, `.err`, and scheduler log files remain configured on EOS.

### Next

- Push and pull this compatibility fix, repeat the dry run, and submit the same resolved smoke config once.
